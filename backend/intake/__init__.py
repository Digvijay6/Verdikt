"""Lane 1 — intake. Application through to interview invite.

  parsing.py     resume PDF -> ParsedResume (Gemini, native document input)
  hard_checks.py deterministic gate, no model
  screening.py   the LLM judgement on survivors
  invites.py     token mint + email
  pipeline.py    orchestrates the above
"""
